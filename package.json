{
  "name": "dislackso",
  "version": "3.4.0",
  "private": true,
  "description": "Servidores privados, compartilhamento de tela em alta resolucao e anotacoes ao vivo",
  "author": "Spike",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/spikeleez/dislackso.git"
  },
  "main": "desktop/main.js",
  "scripts": {
    "start": "node server.js",
    "desktop": "electron .",
    "build": "npm run icon && node scripts/prep-build.js && electron-builder --publish never",
    "portable": "npm run icon && node scripts/pack-portable.js",
    "cert": "node scripts/gen-cert.js",
    "icon": "node scripts/gen-icon.js"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "express": "^4.21.2",
    "selfsigned": "^2.4.1",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8"
  },
  "engines": {
    "node": ">=18"
  },
  "build": {
    "appId": "com.dislackso.desktop",
    "productName": "DiSlackso",
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "spikeleez",
        "repo": "dislackso"
      }
    ],
    "files": [
      "desktop/**/*",
      "server/**/*",
      "public/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        "nsis"
      ],
      "artifactName": "${productName}-Setup-${version}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "shortcutName": "DiSlackso"
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "category": "Network"
    },
    "mac": {
      "target": [
        "dmg"
      ],
      "category": "public.app-category.social-networking"
    }
  }
}
